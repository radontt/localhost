dockers:
  mariadb:
    addon_param: -e MYSQL_ROOT_PASSWORD=admin -e MYSQL_DATABASE=drupal8 -e MYSQL_USER=drupal8 -e MYSQL_PASSWORD=drupal8
    mount_path: mysql:/var/lib/mysql
    repo_name: mariadb
